{
  "name": "post-booster",
  "version": "1.0.0",
  "description": "Generate simple Threads reply suggestions from post links.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "playwright": "^1.53.0"
  }
}
